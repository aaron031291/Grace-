# ===============================
# Grace Governance Kernel Dependencies
# ===============================

# --- Core runtime (standard library, no install needed) ---
# asyncio
# sqlite3
# json
# hashlib
# datetime
# logging
# statistics

# --- API & Server ---
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.0.0
websockets>=12.0

# --- Database Layer ---
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0  # Sync driver for migrations/tools

# --- Redis & Background Workers ---
redis>=5.0.0
celery[redis]>=5.3.0
arq>=0.25.0  # Optional lightweight async task runner

# --- Memory & Vector Store ---
# chromadb>=0.4.0
# qdrant-client>=1.6.0  # Optional: switchable vector DB backend
# sentence-transformers>=2.2.0

# --- Authentication & RBAC ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6
pyjwt>=2.8.0  # Optional JWT handling if needed

# --- Document Processing ---
pypdf>=3.0.0
python-docx>=1.1.0
beautifulsoup4>=4.12.0
pyyaml>=6.0.0  # Policy / config files

# --- AI/LLM Integrations (Optional) ---
# openai>=1.0.0  # External LLM calls if used

# --- Development / Testing (Optional) ---
pytest>=7.0.0
pytest-asyncio>=0.21.0

# --- MLDL Kernel ---
# numpy>=1.21.0
# scikit-learn>=1.0.0
# joblib>=1.0.0

# --- Cloud / Infra (Optional) ---
# boto3>=1.26.0  # AWS SDK for S3 / Secrets Manager / etc.
# prometheus_client>=0.17.0  # Metrics export (Prometheus/Grafana)

# --- Optional System Monitoring ---
# psutil>=5.9.0

# --- Development / Testing (Optional) ---
pytest>=7.0.0
pytest-asyncio>=0.21.0